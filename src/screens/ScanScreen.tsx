import React from 'react';
import ComingSoon from '../components/ComingSoon';

const ScanScreen = () => (
  <ComingSoon
    title="Snap a meal"
    icon="camera-outline"
    blurb="AI estimates with confidence bands and a mandatory confirm step — it never auto-saves."
  />
);

export default ScanScreen;
