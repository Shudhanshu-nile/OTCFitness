import React from 'react';
import ComingSoon from '../components/ComingSoon';

const PaywallScreen = () => (
  <ComingSoon
    title="OTCF Pro"
    icon="star-four-points-outline"
    blurb="The full plan, every training guide and the AI coach without limits — annual by default, trial terms stated plainly."
  />
);

export default PaywallScreen;
